"use client";

// This component is no longer used — generation is handled inline by the wizard.
// Kept as a stub to avoid import errors from any remaining references.
export function GeneratingView(_props: { projectId: string; projectName: string }) {
  return null;
}
