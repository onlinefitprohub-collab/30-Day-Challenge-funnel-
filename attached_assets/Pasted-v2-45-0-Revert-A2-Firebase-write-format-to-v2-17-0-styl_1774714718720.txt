v2.45.0 — Revert A2 Firebase write format to v2.17.0 style (populated dicts, metaData wrapper)

We have confirmed via debug output that v2.17.0 successfully injected sections into the GHL builder. All the debug tooling, diagnostic output, bridge capture, API log, schema diff, roundtrip test, clone baseline — keep all of it exactly as-is. Only the A2 Firebase write payload construction needs to change.

The v2.17.0 working format (what we need to go back to):

rows=7, cols=17, elems=47  ← populated dicts, NOT empty
preWriteRowKeys: ["id","metaData"]  ← wrapped format, NOT flat
rowRefOk=true, colRefOk=true  ← references resolve correctly
firstRowMetaKeys: ["_id","child","element","extra","id","meta","mobileStyles","styles","tagName","title"]

Current broken format:

rows=0, cols=0, elems=0  ← empty dicts — introduced in v2.33.0, WRONG
preWriteRowKeys: ["wrapper","class","customCss","tag",...]  ← flat format — introduced in v2.32.0, WRONG
rowRefOk=false, colRefOk=false  ← broken refs

Specific changes inside _cf_injectViaBuilderSave Approach 2 only:

Remove flattenForFirebase — stop spreading metaData to top level. Write rows, columns, and elements in their original {id, metaData:{...}} wrapper format from pageData.

Write populated dicts — rows, columns, elements must be the full objects from pageData, not empty {}.

Remove the flat element tree from sections — sections should NOT have a section.elements array. They should only have id, metaData (with child refs), sequence, pageId, funnelId, locationId, general. Element data lives in the rows/cols/elements dicts.

writePayload should be:

const writePayload = {
  ...pd,
  sections: sectionsWithContext,
  rows:     pd.rows,      // full populated dict
  columns:  pd.columns,   // full populated dict
  elements: pd.elements,  // full populated dict
  id:       builderId,
};

Do not touch:

All debug diagnostic collection (diag fields, postWrite checks, etc.)
Roundtrip test, schema diff, clone baseline, API log
bridge.js, popup.js display logic
A0, A1, A2B, A3, A4
Any version strings in ghl-inspector.tsx
Bump version to v2.45.0 in manifest.json, popup.js, background.js, ghl-inspector.tsx only.