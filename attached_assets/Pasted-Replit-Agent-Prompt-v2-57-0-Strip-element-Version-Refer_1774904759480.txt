Replit Agent Prompt — v2.57.0: Strip element Version References from Write Payload

Context

v2.56.0 confirmed: Firebase write succeeds, token patched, auto-reload fires. The page shows a mix of old content and new because the Firebase payload contains element fields in section metaData and in individual elements — these are GHL version-history snapshots of the original page before the public template was applied. GHL's builder renders these as an overlay of the old page alongside the new structure.

The fix — strip element fields after deep-cloning sections, before writing:

In background.js, inside the write-in-place block, after the line:

var clonedSections = JSON.parse(JSON.stringify(existing.sections));

Add a recursive strip pass immediately after:

function stripElementFields(obj) {
  if (Array.isArray(obj)) {
    for (var i = 0; i < obj.length; i++) stripElementFields(obj[i]);
  } else if (obj !== null && typeof obj === 'object') {
    delete obj.element;
    var keys = Object.keys(obj);
    for (var k = 0; k < keys.length; k++) {
      if (keys[k] !== 'element') stripElementFields(obj[keys[k]]);
    }
  }
}
stripElementFields(clonedSections);

Rules:

Must use function declaration and var — no const/let/arrow functions (inside executeScript body)
Must run AFTER the deep-clone and BEFORE the AI text replacement walk
Deletes element at every level: section metaData, individual elements in the flat array, nested objects within elements
Does NOT delete elements (the array field on sections) — only the singular element field
Add diag field:

After the strip, add:

diag.approach2.strippedElementFields = true;

And display it in popup.js alongside the other A2 fields.

Also note — replaceableCount: 51 vs replacedCount: 39:

The 12-section public template has 51 text-replaceable elements but aiTextCount is only 39. The current sequential replacement correctly fills the first 39 and leaves the remaining 12 as template placeholder text. This is acceptable for v2.57.0 — no change needed to the replacement logic.

Version bump to 2.57.0 — all four locations: manifest.json, background.js, popup.js, ghl-inspector.tsx.

Done looks like:

Click Inject
strippedElementFields: true appears in popup debug
GHL builder auto-reloads
Page shows ONLY the public template structure with AI text — no bleed-through from the original CcigoVKGE5W3HNmjTXeS page content
No mixed/hybrid layout
Out of scope:

Filling the remaining 12 un-replaced template text slots (AI text count vs replaceable count mismatch)
Schema Diff simulation update
Any changes to bridge.js, content.js, roundtrip test