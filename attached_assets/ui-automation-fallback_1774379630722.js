import { logger } from "../../core/logger";
// TODO: UI automation fallback for when direct state injection isn't possible
export function newBuilderUiAutomation() {
    logger.info("new-builder-ui", "UI automation fallback stub invoked");
    return false;
}
//# sourceMappingURL=ui-automation-fallback.js.map