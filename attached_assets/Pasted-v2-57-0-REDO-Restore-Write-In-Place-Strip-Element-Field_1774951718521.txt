v2.57.0 REDO: Restore Write-In-Place + Strip Element Fields

CRITICAL — what happened in the last build was wrong:

The previous build replaced the working Firebase write-in-place inject with a clipboard-based approach (writing to GHL's clipboard and asking the user to press Ctrl+V). This must be completely removed. The clipboard approach does not work in the GHL page builder and must never be used. Delete all clipboard-related code added in the last build.

What v2.57.0 must do — restore v2.56.0 behaviour exactly, plus one addition:

The working inject flow from v2.56.0 (confirmed working) is:

User clicks "Paste into GHL Builder" button in the extension popup
The inject reads the current page's Firebase data (existing — already fetched at the probe step in _cf_injectViaBuilderSave)
Deep-clones existing.sections
Strips element (singular) fields — this is the only new addition vs v2.56.0 (see below)
Walks cloned sections' elements and replaces extra.text.value with AI texts from pd sequentially
Writes the modified payload back to the SAME Firebase path (objectPath from existing.downloadUrl)
Patches the old download token back (format-1 PATCH — already working)
Sets diag.approach2.clonePatchToken, diag.approach2.cloneWriteStatus, etc.
Calls setTimeout(function() { window.location.reload(); }, 400) — auto-reloads the GHL builder tab
Returns JSON.stringify({ ok: true, method: 'write-in-place', diag: diag })
No clipboard. No Ctrl+V prompt. No navigation to a new URL. No new funnel steps.

The one new addition (Task 1):

Inside _cf_injectViaBuilderSave, in the write-in-place block, immediately after:

var clonedSections = JSON.parse(JSON.stringify(wipPayload.sections));

Insert:

function stripElementFields(obj) {
  if (Array.isArray(obj)) {
    for (var i = 0; i < obj.length; i++) stripElementFields(obj[i]);
  } else if (obj !== null && typeof obj === 'object') {
    delete obj.element;
    var keys = Object.keys(obj);
    for (var k = 0; k < keys.length; k++) {
      if (keys[k] !== 'element') stripElementFields(obj[keys[k]]);
    }
  }
}
stripElementFields(clonedSections);
diag.approach2.strippedElementFields = true;

Rules:

function declaration and var only — no const, let, or arrow functions (inside executeScript body)
Runs AFTER the deep-clone, BEFORE the AI text replacement walk
Deletes element (singular) at every nesting depth — section metaData, individual elements in the flat array, nested objects
Does NOT delete elements (plural — the array field on sections)
Task 2 — popup.js:

Remove any message about clipboard or Ctrl+V. The popup should only show the standard diag fields. Add strippedElementFields display alongside the other A2 write-in-place fields.

The popup button "Paste into GHL Builder" triggers the inject — it does NOT write to clipboard.

Task 3 — version bump to 2.57.0:

All four locations: manifest.json, background.js, popup.js, ghl-inspector.tsx.

Done looks like:

User opens GHL page builder on a valid existing page
User clicks "Paste into GHL Builder"
Popup shows: ok=true | method=write-in-place, strippedElementFields: true, replacedCount > 0, cloneWriteStatus: 200, clonePatchToken: ok-format1
GHL builder tab auto-reloads ~400ms later
Page shows public template structure with AI copy — no old content bleed-through
NO clipboard prompt. NO Ctrl+V message. NO new funnel step.
Also note: The current test page OmiRNLJ7h9UeaCxILEKO returns 400 "Page does not exist or is deleted" from GHL — this page has been deleted. Before testing v2.57.0, open a valid existing GHL page in the builder. Use CcigoVKGE5W3HNmjTXeS or any other live page.